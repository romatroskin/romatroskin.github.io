---
phase: 12-final-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/HeroSection/HeroSection.tsx
  - src/components/sections/HeroSection/index.ts
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "Hero section renders identically in browser (no visual regression)"
    - "Scroll-to-section navigation from hero button still works"
    - "App.tsx is more maintainable with extracted section"
    - "Build and tests pass"
  artifacts:
    - path: "src/components/sections/HeroSection/HeroSection.tsx"
      provides: "Extracted hero section component"
      exports: ["default"]
      min_lines: 30
    - path: "src/components/sections/HeroSection/index.ts"
      provides: "Barrel export for HeroSection"
      contains: "export { default"
    - path: "src/App.tsx"
      provides: "Simplified App with HeroSection import"
      contains: "HeroSection"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/sections/HeroSection/HeroSection.tsx"
      via: "inline import (not lazy - above fold)"
      pattern: "import HeroSection"
    - from: "src/components/sections/HeroSection/HeroSection.tsx"
      to: "src/App.tsx"
      via: "onNavigate prop callback"
      pattern: "onNavigate\\("
---

<objective>
Extract Hero section from App.tsx into dedicated HeroSection component following established section patterns.

Purpose: Improve maintainability by extracting inline section to match Services/About/Contact pattern (Phase 12 success criteria #3)
Output: HeroSection component in src/components/sections/ with App.tsx simplified
</objective>

<execution_context>
@/Users/romatroskin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romatroskin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/App.tsx (lines 299-328 contain hero section inline in ParallaxLayer)
@src/components/sections/ServicesSection/ServicesSection.tsx (reference pattern)
@src/components/sections/AboutSection/AboutSection.tsx (reference pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HeroSection Component</name>
  <files>
    src/components/sections/HeroSection/HeroSection.tsx
    src/components/sections/HeroSection/index.ts
  </files>
  <action>
Create HeroSection component following established section patterns:

1. Create directory: `src/components/sections/HeroSection/`

2. Create `HeroSection.tsx`:
```typescript
interface HeroSectionProps {
  onNavigate: (page: number) => void;
}

export default function HeroSection({ onNavigate }: HeroSectionProps) {
  return (
    <section id="hero-section" aria-labelledby="hero-heading">
      <div className="hero-container content-card">
        <a href="#" target="_blank" rel="noreferrer">
          <img
            src="https://raw.githubusercontent.com/PuffPuffDev/puff_puff_brand/main/logos/logo_white.svg"
            srcSet="
              https://raw.githubusercontent.com/PuffPuffDev/puff_puff_brand/main/logos/logo_white.svg 1x,
              https://raw.githubusercontent.com/PuffPuffDev/puff_puff_brand/main/logos/logo_white.svg 2x
            "
            className="logo"
            alt="Puff Puff logo"
            width="200"
            height="200"
          />
        </a>
        <h1 id="hero-heading" className="hero-title">
          Where Code Meets Creativity,
          <br />
          <span className="hero-highlight">Dreams Take Shape.</span>
        </h1>
        <button
          className="scroll-indicator"
          onClick={() => onNavigate(1)}
          aria-label="Scroll to next section"
        >
          <span className="scroll-arrow">↓</span>
          <span className="scroll-text">Discover More</span>
        </button>
      </div>
    </section>
  );
}
```

3. Create barrel export `index.ts`:
```typescript
export { default } from './HeroSection';
export { default as HeroSection } from './HeroSection';
```

Note: HeroSection is NOT lazy-loaded because it's above the fold (Page 1 - critical for LCP).
This matches ServicesSection/AboutSection pattern but without lazy loading.
  </action>
  <verify>Files created with correct structure</verify>
  <done>HeroSection component created in sections folder</done>
</task>

<task type="auto">
  <name>Task 2: Update App.tsx to Use HeroSection</name>
  <files>src/App.tsx</files>
  <action>
Update App.tsx to import and use HeroSection:

1. Add import at top (after other section imports, NOT lazy):
```typescript
import HeroSection from "@/components/sections/HeroSection";
```

2. Replace inline hero section JSX (lines 299-328) with:
```typescript
<HeroSection onNavigate={scrollToPage} />
```

The ParallaxLayer wrapper remains in App.tsx:
```typescript
<ParallaxLayer
  offset={0}
  speed={0.2 * animationParams.parallaxSpeedMultiplier}
  style={{
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  }}
>
  <HeroSection onNavigate={scrollToPage} />
</ParallaxLayer>
```

This matches the pattern used for ServicesSection, AboutSection, ContactSection but WITHOUT Suspense/lazy since Hero is above the fold.
  </action>
  <verify>npm run build && npm run dev (verify hero renders correctly)</verify>
  <done>App.tsx updated to use HeroSection component</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm run dev` shows hero section rendering correctly
3. Click "Discover More" button scrolls to Services section
4. App.tsx line count reduced by ~25-30 lines
5. HeroSection follows same pattern as other section components
</verification>

<success_criteria>
- HeroSection component exists in src/components/sections/HeroSection/
- App.tsx imports and uses HeroSection
- No visual regression in hero section
- Scroll navigation still works
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-final-cleanup/12-02-SUMMARY.md`
</output>
