---
phase: 09-contact-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/components/sections/ContactSection/ContactSection.tsx
  - src/components/sections/ContactSection/ContactSection.module.css
autonomous: true
user_setup:
  - service: formspree
    why: "Contact form backend for receiving submissions"
    env_vars:
      - name: FORMSPREE_FORM_ID (or hardcode in component)
        source: "Formspree Dashboard -> Create Form -> Copy Form ID"
    dashboard_config:
      - task: "Create Formspree account at formspree.io"
        location: "https://formspree.io/register"
      - task: "Create new form and copy Form ID (format: xyzabcde)"
        location: "Formspree Dashboard -> Forms -> New Form"
      - task: "Add production domain to authorized domains (optional but recommended)"
        location: "Formspree Dashboard -> Form Settings -> Restrictions"

must_haves:
  truths:
    - "ContactSection component exists and exports default function"
    - "@formspree/react is installed and available for import"
    - "react-social-icons is installed for GitHub/LinkedIn icons"
    - "CSS Modules file exists with card-based styling"
  artifacts:
    - path: "src/components/sections/ContactSection/ContactSection.tsx"
      provides: "Contact section component skeleton with exports"
      exports: ["default"]
    - path: "src/components/sections/ContactSection/ContactSection.module.css"
      provides: "Scoped styles for contact section"
      contains: ".contactCard"
  key_links:
    - from: "package.json"
      to: "@formspree/react, react-social-icons"
      via: "dependencies"
      pattern: "@formspree/react"
---

<objective>
Set up contact section foundation with npm dependencies and component skeleton.

Purpose: Create the base infrastructure for the contact section before implementing form logic and integration. This separates dependency installation from feature development.

Output: A working ContactSection component skeleton with CSS Modules styling, plus @formspree/react and react-social-icons available as dependencies.
</objective>

<execution_context>
@/Users/romatroskin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/romatroskin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-contact-section/09-CONTEXT.md
@.planning/phases/09-contact-section/09-RESEARCH.md
@src/components/sections/AboutSection/AboutSection.tsx
@src/App.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install contact section dependencies</name>
  <files>package.json, package-lock.json</files>
  <action>
    Install the required npm packages for the contact section:

    ```bash
    npm install @formspree/react react-social-icons
    ```

    This adds:
    - @formspree/react: Official Formspree React library with useForm hook
    - react-social-icons: Pre-made SVG social icons for GitHub/LinkedIn

    After installation, verify packages appear in package.json dependencies.
  </action>
  <verify>
    Run `npm list @formspree/react react-social-icons` - should show both packages installed.
    Check package.json contains both in dependencies section.
  </verify>
  <done>
    package.json shows @formspree/react and react-social-icons in dependencies.
    npm install completed without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ContactSection component structure</name>
  <files>
    src/components/sections/ContactSection/ContactSection.tsx
    src/components/sections/ContactSection/ContactSection.module.css
  </files>
  <action>
    Create the ContactSection folder and files following existing section patterns.

    1. Create directory: `src/components/sections/ContactSection/`

    2. Create `ContactSection.tsx` with skeleton structure:
    ```typescript
    import styles from './ContactSection.module.css';

    export default function ContactSection() {
      return (
        <section id="contact-section" aria-labelledby="contact-heading">
          <div className={`${styles.contactCard} content-card`}>
            <h2 id="contact-heading" className="section-title">Get In Touch</h2>
            {/* Form will be implemented in next plan */}
            <p className={styles.placeholder}>Contact form coming soon...</p>
          </div>
        </section>
      );
    }
    ```

    Note: Must use `export default function` (not `export const`) for React.lazy compatibility.

    3. Create `ContactSection.module.css` with base styles:
    ```css
    /* Contact Section - Card-based layout */
    .contactCard {
      max-width: 600px;
      width: 100%;
      text-align: left;
    }

    .placeholder {
      color: var(--color-text-secondary);
      text-align: center;
      padding: 2rem 0;
    }

    /* Form styles (to be populated in next plan) */
    .form {
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
      margin-top: 2rem;
    }

    .formField {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
    }

    .formLabel {
      font-weight: 500;
      color: var(--color-text-primary);
    }

    .formInput,
    .formTextarea {
      padding: 0.75rem;
      min-height: 44px;
      border: 1px solid var(--color-border);
      border-radius: 0.5rem;
      background: var(--color-bg-primary);
      color: var(--color-text-primary);
      font-size: 1rem;
      transition: border-color var(--transition-fast);
    }

    .formInput:focus,
    .formTextarea:focus {
      outline: 2px solid var(--color-accent);
      outline-offset: 2px;
      border-color: var(--color-accent);
    }

    .formInput[aria-invalid="true"],
    .formTextarea[aria-invalid="true"] {
      border-color: #ef4444;
    }

    .formTextarea {
      min-height: 120px;
      resize: vertical;
    }

    .errorMessage {
      color: #ef4444;
      font-size: 0.875rem;
    }

    .submitButton {
      align-self: flex-start;
    }

    /* Social links */
    .socialLinks {
      display: flex;
      gap: 1rem;
      justify-content: center;
      margin-top: 2rem;
      padding-top: 1.5rem;
      border-top: 1px solid var(--color-border);
    }

    .socialLink {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      min-width: 44px;
      min-height: 44px;
      transition: transform var(--transition-fast);
    }

    .socialLink:hover {
      transform: scale(1.1);
    }

    /* Success message */
    .successMessage {
      text-align: center;
      padding: 2rem;
    }

    .successMessage h3 {
      color: var(--color-text-primary);
      margin-bottom: 1rem;
    }

    .successMessage p {
      color: var(--color-text-secondary);
    }
    ```

    The component follows the AboutSection pattern with:
    - Semantic section with aria-labelledby
    - content-card class for glass-morphism styling
    - section-title class for heading
    - Default export for React.lazy
  </action>
  <verify>
    - File exists: `src/components/sections/ContactSection/ContactSection.tsx`
    - File exists: `src/components/sections/ContactSection/ContactSection.module.css`
    - TypeScript compiles: `npx tsc --noEmit`
  </verify>
  <done>
    ContactSection component skeleton renders without errors.
    CSS Module file exists with styles for card, form, and social links.
    Component uses default export for React.lazy compatibility.
  </done>
</task>

</tasks>

<verification>
1. Run `npm list @formspree/react react-social-icons` - both packages listed
2. Run `npx tsc --noEmit` - no TypeScript errors
3. Verify files exist in `src/components/sections/ContactSection/`
4. Component imports resolve correctly
</verification>

<success_criteria>
- Both npm packages installed successfully
- ContactSection component compiles without errors
- CSS Module file contains card-based styling
- Component uses default export for React.lazy compatibility
- Build succeeds: `npm run build`
</success_criteria>

<output>
After completion, create `.planning/phases/09-contact-section/09-01-SUMMARY.md`
</output>
